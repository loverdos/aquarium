<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%-5level %d{HH:mm:ss.SSS} [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${AQUARIUM_HOME}/logs/aquarium.log</file>
    <append>true</append>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${AQUARIUM_HOME}/logs/aquarium.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>
    <encoder>
      <pattern>%-5level %d{HH:mm:ss.SSS} [%thread] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="ch.qos.logback" level="DEBUG"/>

  <logger name="com.ckkloverdos" level="INFO"/>

  <logger name="cc.spray.can" level="DEBUG"/>

  <logger name="gr.grnet" level="DEBUG"/>

  <root level="DEBUG">
    <appender-ref ref="FILE"/>
  </root>
</configuration>
